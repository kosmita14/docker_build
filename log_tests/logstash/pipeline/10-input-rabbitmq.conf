input {
    rabbitmq {
        host => "localhost"
        queue => "dupa"
        exchange => "test_exchange"
        exchange_type => "topic"
        durable => "true"    
        key => "#"
    }
}
