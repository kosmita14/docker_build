version: '3'

services:
  elasticsearch:
    image: elasticsearch
    volumes:
#      - ./elasticsearch/config/elasticsearch.yml:/usr/share/elasticsearch/config/elasticsearch.yml
       - /media/pi/ext250/elastic_data:/usr/share/elasticsearch/data 
    ports:
      - "9200:9200"
      - "9300:9300"
    environment:
        ES_JAVA_OPTS: "-Xmx256m -Xms256m"

#docker run -d --name some-elasticsearch 
#-v /media/pi/ext250/elastic_data:/usr/share/elasticsearch/data -p 9200:9200 -p 9300:9300 elasticsearch
